# Phase 276-295 Pilot Selection

## Chosen Pilot

### Lacrimosa

Chosen because:

- Candidate confidence was `medium`.
- The candidate contains character-specific combat/skill/rotation notes.
- Conflicts are mixed-source context, not a live canonical-field apply.
- Notes can improve the detail page without changing identity, stats, formulas, or recommendations.
- The page is currently sparse enough that a source-pending notes section adds value.

## Fields Exposed

Only optional source-pending notes are exposed:

- Combat Notes
- Skill Notes
- Rotation Notes
- Warning copy that the corpus contains OCR/scrape artifacts

No raw file paths, source URLs, exact values, formulas, official claims, or verified labels are shown publicly.

## Rejected / Deferred Candidates

- `nanally`: compare-only; existing structured page is stronger.
- `daffodil`: no conflicts, but generated candidate is mostly metadata and not useful player-facing content.
- `hanizel`: deferred because Haniel/Hanizel naming needs manual review.
- `hotori`: deferred because useful-looking text is mixed with Haniel and comment/aggregate content.
- `adler`, `aurelia`, `baicang`, `chiz`, `edgar`, `fadia`, `hathor`, `jiuyuan`, `mint`, `sakiri`, `skia`: rejected for now due mixed-source or protected-field conflict risk.

## Safety Notes

- Canonical fields overwritten: no
- Nanally overwritten: no
- Build Planner runtime changed: no
- Backend/schema/auth/admin changed: no
- Public data status: `needs_verification`
